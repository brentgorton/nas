# Debian 13 (Trixie) Preseed Configuration
# Automated minimal installation for NAS system

### Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

### Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string nas
d-i netcfg/get_domain string local

### Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Account setup
d-i passwd/root-login boolean false
d-i passwd/user-fullname string NAS Admin
d-i passwd/username string nas
# Password is "password" - should be changed on first login
d-i passwd/user-password password password
d-i passwd/user-password-again password password

### Clock and time zone setup
d-i clock-setup/utc boolean true
d-i time/zone string UTC

### Partitioning
d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Base system installation
d-i base-installer/install-recommends boolean false

### Apt setup
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/non-free-firmware boolean true
d-i apt-setup/non-free boolean false
d-i apt-setup/contrib boolean false
d-i apt-setup/services-select multiselect security, updates
d-i apt-setup/security_host string security.debian.org

### Package selection
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string sudo openssh-server mergerfs snapraid samba nfs-kernel-server
d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

### Boot loader installation
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean false
d-i grub-installer/bootdev string default

### Finishing up the installation
d-i finish-install/reboot_in_progress note
d-i debian-installer/exit/poweroff boolean false

### Late command - runs at the end of installation
d-i preseed/late_command string \
    in-target sh -c 'echo "nas ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/nas'; \
    in-target chmod 440 /etc/sudoers.d/nas; \
    in-target systemctl enable ssh; \
    cp /cdrom/custom-packages/*.deb /target/tmp/ 2>/dev/null || true; \
    in-target sh -c 'dpkg -i /tmp/*.deb 2>/dev/null || apt-get install -f -y'; \
    in-target rm -f /tmp/*.deb
